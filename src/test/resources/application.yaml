
logging:
  level:
    org.springframework: ERROR
    io.netty.util: ERROR
    cloud.sonam: DEBUG

  pattern:
    console: "%d{HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n"
    file: "%d %p %c{1.} [%t] %m%n"
  file: app.log

spring:
  codec:
    max-in-memory-size: 10MB